# No environment variables are required by the current downloads stack.
# FluxDown and xunlei settings are defined in docker-compose.yml.

# Optional xunlei dashboard credentials (currently commented out in compose):
# XL_DASHBOARD_USERNAME=
# XL_DASHBOARD_PASSWORD=
